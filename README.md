
  # Agent Creation Screen

  This is a code bundle for Agent Creation Screen. The original project is available at https://www.figma.com/design/KlSZnbLQk0HF187b20qrd0/Agent-Creation-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  